# Border Protocol — Python dependencies
# Install with: pip install -r requirements.txt
# Or install the package itself: pip install -e .

# Web frameworks
flask>=3.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP clients
requests>=2.31.0
httpx>=0.27.0

# Cryptography (Ed25519, ChaCha20-Poly1305, HKDF, X25519, scrypt)
cryptography>=42.0.0

# Data validation (used by FastAPI subsystems)
pydantic>=2.7.0

# ── Optional ──────────────────────────────────────────────────────────────
# GPU monitoring (for compute/daemon.py)
# pynvml>=11.5.0

# LoRa radio interface (for border/lora.py)
# pyserial>=3.5
